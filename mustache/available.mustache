<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Staffing Directory</title>
  <link rel="stylesheet" href="style.css"/>
</head>

<body>
  <h1 class="header">RoboNerds looking for Human jobs</h1>
  <div class = "availablewrapper">
    {{ #robots }}
    <div class="robotwrapper">
      <div><img src="{{ avatar }}" class="availablephoto"></div>
      <div class="info">
        <span><h1>{{ name }}</h1></span>
        <span><p>{{ #address.city }}{{ address.city }},{{ /address.city }} {{ address.country }} | {{ #job }} {{ job }}, {{ /job }} {{ ^job }} <span class= "available"> Available for hire</span>{{ /job }} {{ company }}</p></span>
        <span><p>{{ email }} | {{ phone }}</p></span>
      </div>
    </div>
  <div class="attributes">
    <div class="category">
      <span>
      <h4>Education</h4>
      <h4>Skills</h4>
    </span>
    </div>
    <div class="skillwrapper">
      <p>{{ university }}</p>
      {{ #skills }}
      <ul>
        <li>{{ . }}</li>
      </ul>
      {{ /skills }}
    </div>
  </div>
    {{ /robots }}
  </div>
</body>
</html>
